import { StyleSheet } from 'react-native';

export const chipStyles = StyleSheet.create({
  chip: {
    borderWidth: 1,
    borderRadius: 100,
    borderColor: 'teal',
    paddingVertical: 4,
    paddingHorizontal: 8,
  },
  chipText: {
    fontSize: 15,
    fontWeight: '600',
    color: 'teal',
  },
});
