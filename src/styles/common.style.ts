import { StyleSheet } from 'react-native';

export const commonStyles = StyleSheet.create({
  container: {
    padding: 16,
    gap: 14,
  },
  textHeading: {
    fontSize: 18,
    fontWeight: '700',
  },
  textBody: {
    fontSize: 16,
    fontWeight: '500',
  },
});
